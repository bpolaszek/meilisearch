---
phase: 03-integration-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/meilisearch/tests/auth/tenant_token.rs
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05

must_haves:
  truths:
    - "searchRules filter behavior is unchanged when indexRules is also present — search results are scoped by searchRules, NOT by indexRules"
    - "indexRules null-rule (whitelisted index, no filter) returns all documents, not an empty set or 403"
    - "indexRules filter and caller-supplied filter are ANDed together — only the intersection is returned"
  artifacts:
    - path: "crates/meilisearch/tests/auth/tenant_token.rs"
      provides: "3 new integration tests appended after line 984"
      contains: "search_rules_filter_unaffected_by_index_rules"
  key_links:
    - from: "search_rules_filter_unaffected_by_index_rules"
      to: "index.search() with searchRules + indexRules token"
      via: "result count assertion (1 hit for searchRules scope, not 2 from indexRules)"
      pattern: "response.*hits.*len.*1"
    - from: "index_rules_filter_fused_with_query_filter"
      to: "index.fetch_documents(json!({\"filter\": \"id > 1\"}))"
      via: "index.fetch_documents() helper (index.rs:506) — sends POST /documents/fetch with JSON body containing caller filter, combined with indexRules filter server-side"
      pattern: "results.*len.*1"
    - from: "index_rules_null_rule_returns_all_documents"
      to: "GET /documents with indexRules null-rule token"
      via: "result count assertion (all 3 documents)"
      pattern: "results.*len.*3"
---

<objective>
Add 3 integration tests to verify end-to-end security properties of `indexRules` that are not yet covered by Phase 2 tests.

Purpose: Phase 2 tests cover success criteria 1-4 but criterion 5 (searchRules filter independence) only checks HTTP 200, not result counts. Two additional edge cases (null-rule whitelist, filter fusion) flagged in Phase 2 VERIFICATION.md also lack automated coverage.

Output: 3 new passing tests in `crates/meilisearch/tests/auth/tenant_token.rs`
</objective>

<execution_context>
@/Users/bpolaszek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bpolaszek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-integration-tests/03-RESEARCH.md

<interfaces>
<!-- All helpers already available in tenant_token.rs — no new imports needed -->

From crates/meilisearch/tests/auth/tenant_token.rs:
```rust
fn generate_tenant_token(uid: &str, key: &str, body: HashMap<&str, Value>) -> String;
// Server helpers (from tests/common/server.rs, already imported):
// server.new_auth().await, server.use_admin_key(), server.use_api_key()
// server.add_api_key(), server.dummy_request(method, url), server.wait_task()
// index.search(json, callback), index.add_documents(), index.update_settings()
// index.fetch_documents(payload: Value) -> (Value, StatusCode)  ← POST /documents/fetch with JSON body
```

Key helper details (verified from codebase):
- `server.dummy_request("POST", url)` sends an EMPTY body `json!({})` — it CANNOT pass a filter payload
- `index.fetch_documents(payload)` sends `POST /indexes/{uid}/documents/fetch` with the given JSON body — USE THIS for filter fusion test

Existing Phase 2 tests (DO NOT duplicate — these already pass):
- `index_rules_list_filtered` — Crit 1: cross-tenant access blocked
- `index_rules_fetch_filtered` — Crit 1: POST fetch filtered (uses dummy_request with empty body)
- `index_rules_single_doc_out_of_scope` — Crit 4: 404 for out-of-scope doc
- `index_rules_fail_closed` — Crit 2: 403 without indexRules
- `index_rules_admin_key_unaffected` — Crit 3: admin key unfiltered

Partial coverage (Phase 1):
- `index_rules_independent_from_search_rules` — checks HTTP 200 only, NOT result count
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 3 integration tests for security edge cases</name>
  <files>crates/meilisearch/tests/auth/tenant_token.rs</files>
  <behavior>
    - Test 1 (search_rules_filter_unaffected_by_index_rules): With searchRules scoped to tenant_id=b and indexRules scoped to tenant_id=a, a search request returns exactly 1 hit (tenant_id=b), NOT 2 (tenant_id=a cross-contamination) and NOT 0 (double filtering)
    - Test 2 (index_rules_filter_fused_with_query_filter): With indexRules tenant_id=a (allows docs 1,2) and caller-supplied filter id > 1, POST /documents/fetch returns exactly 1 result (doc 2) — both filters ANDed
    - Test 3 (index_rules_null_rule_returns_all_documents): With indexRules {"sales": null} (whitelist, no filter), GET /documents returns all 3 documents — not 0 and not 403
  </behavior>
  <action>
Append 3 new test functions after line 984 of `crates/meilisearch/tests/auth/tenant_token.rs`. Follow the exact boilerplate pattern used by Phase 2 tests (see RESEARCH.md Pattern 1).

**Test 1: `search_rules_filter_unaffected_by_index_rules`** (Criterion 5)
- Setup: 3 docs (id:1 tenant_id:a, id:2 tenant_id:a, id:3 tenant_id:b), filterableAttributes: ["tenant_id"]
- Token: searchRules {"sales": {"filter": "tenant_id = b"}}, indexRules {"sales": {"filter": "tenant_id = a"}}
- Use `server.use_api_key(&token)` then `server.index("sales")` then `index.search(json!({}), callback)`
- Assert: code == 200 AND hits.len() == 1 (tenant_id=b only, not cross-contaminated by indexRules tenant_id=a)
- IMPORTANT: Use `index.search()` not `dummy_request` for search assertions (see RESEARCH.md Pattern 2)

**Test 2: `index_rules_filter_fused_with_query_filter`** (VERIFICATION.md gap)
- Setup: same 3 docs, filterableAttributes: ["tenant_id", "id"] (need "id" filterable for the query filter)
- Token: indexRules {"sales": {"filter": "tenant_id = a"}} (allows docs 1, 2)
- Use `index.fetch_documents(json!({"filter": "id > 1"}))` — this is the `Index::fetch_documents` helper (index.rs:506) which sends `POST /indexes/sales/documents/fetch` with the JSON body `{"filter": "id > 1"}`. Do NOT use `dummy_request` which sends an empty body and cannot pass a filter payload.
- Assert: results.len() == 1 AND results[0]["id"] == 2 (indexRules keeps docs 1,2; caller filter keeps id>1; intersection = doc 2 only)

**Test 3: `index_rules_null_rule_returns_all_documents`** (VERIFICATION.md gap)
- Setup: same 3 docs, filterableAttributes: ["tenant_id"]
- Token: indexRules {"sales": null} (null = whitelist, no filter)
- Use `dummy_request("GET", "/indexes/sales/documents")`
- Assert: code == 200 AND results.len() == 3

All 3 tests must compile and pass. Run `cargo test -p meilisearch --test auth -- search_rules_filter_unaffected_by_index_rules index_rules_filter_fused_with_query_filter index_rules_null_rule_returns_all_documents` to verify.

Note: for the filter fusion test (Test 2), the `id` field must be added to `filterableAttributes` since the caller-supplied filter uses `id > 1`. Use `filterableAttributes: ["tenant_id", "id"]` in that test's setup.
  </action>
  <verify>
    <automated>cargo test -p meilisearch --test auth -- search_rules_filter_unaffected_by_index_rules index_rules_filter_fused_with_query_filter index_rules_null_rule_returns_all_documents</automated>
  </verify>
  <done>All 3 tests pass. Criterion 5 has result-count coverage. Null-rule and filter-fusion edge cases are automated. No existing tests broken.</done>
</task>

</tasks>

<verification>
1. All 3 new tests pass: `cargo test -p meilisearch --test auth -- search_rules_filter_unaffected_by_index_rules index_rules_filter_fused_with_query_filter index_rules_null_rule_returns_all_documents`
2. All existing index_rules tests still pass: `cargo test -p meilisearch --test auth -- index_rules`
3. Full auth test suite green: `cargo test -p meilisearch --test auth`
</verification>

<success_criteria>
- search_rules_filter_unaffected_by_index_rules passes with result-count assertion (1 hit, not just HTTP 200)
- index_rules_filter_fused_with_query_filter passes proving both filters are ANDed
- index_rules_null_rule_returns_all_documents passes proving null-rule returns all docs
- No regressions in existing auth test suite
</success_criteria>

<output>
After completion, create `.planning/phases/03-integration-tests/03-01-SUMMARY.md`
</output>
